/** @type {import('tailwindcss').Config} */
module.exports = {
  content: [
    "./HTML/**/*.html", // ← all HTML files in HTML/
    "./src/**/*.html", // ← all HTML files in src/
  ],
  theme: {
    extend: {},
  },
  plugins: [],
};
